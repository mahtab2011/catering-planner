{
  "name": "london-food-hubs-functions",
  "description": "Trusted server-side operations that Firestore rules alone cannot express: granting the admin custom claim, and maintaining the restaurant rating aggregate.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "true"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
